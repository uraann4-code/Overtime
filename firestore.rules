rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DIRECT ACCESS MODE
    // Warning: This allows anyone to read and write to the database.
    // This is for development purposes only as requested.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
